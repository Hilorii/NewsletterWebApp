@model IEnumerable<NewsletterWebApp.ViewModels.UserViewModel>

@{
    ViewData["Title"] = "Home Page";
}



<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h1>Lista użytkowników</h1>
</div>
@foreach (var user in Model)
{
    <h2>@user.Email</h2>
}